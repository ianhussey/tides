% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_umbrella.R
\name{plot_umbrella}
\alias{plot_umbrella}
\title{Create an umbrella plot of feasible means and SDs}
\usage{
plot_umbrella(dat, size = 0.5)
}
\arguments{
\item{dat}{A tibble containing at least the columns
\describe{
\item{mean}{Numeric: a feasible reported mean.}
\item{sd}{Numeric: a feasible reported standard deviation.}
\item{digits}{Integer: number of decimal places used,
for setting axis expansion.}
}}

\item{size}{Numeric. Point size (in mm) passed to \code{\link[ggplot2]{geom_point}()};
defaults to 0.5.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object: a scatterplot of \code{sd} vs.\ \code{mean}
with square markers, appropriately scaled axes, and a linedraw theme.
}
\description{
Given a tibble of (mean, SD) pairs that pass GRIM, GRIMMER, and TIDES
consistency tests (as returned by \code{\link{umbrella}()}), produce
a square-point scatterplot of SD vs. mean.
}
\examples{
\donttest{
df <- umbrella(n = 14, min = 1, max = 7, n_items = 1, digits = 2)
plot_umbrella(df)
plot_umbrella(df, size = 1)
}

}
