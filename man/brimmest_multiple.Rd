% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brimmest.R
\name{brimmest_multiple}
\alias{brimmest_multiple}
\title{Certify many reported (mean, SD) rows exactly}
\usage{
brimmest_multiple(data, ..., include_inputs = TRUE)
}
\arguments{
\item{data}{A data frame, one reported statistic set per row.}

\item{...}{Constant arguments applied to all rows.}

\item{include_inputs}{If TRUE (default), returns \code{data} column-bound to the
results; if FALSE, only the result columns (same row order), for drop-in
use inside a \code{dplyr::mutate()} / \code{purrr} pipeline.}
}
\value{
A data frame with \code{possible} (logical) and \code{rules} (the rounding
rules under which the row is reachable, comma-separated and \code{""} when
none), optionally with \code{data} prepended. Unlike \code{\link[=brimmest]{brimmest()}}, the reported
\code{mean} and \code{sd} are not echoed back: they are already columns of \code{data}
when supplied that way, and repeating them would collide.
}
\description{
Applies \code{\link[=brimmest]{brimmest()}} to each row of a data frame. Columns of \code{data} whose
names match \code{\link[=brimmest]{brimmest()}} arguments are taken per row; any argument given in
\code{...} is a constant broadcast to every row. Supplying one name both ways is
an error. The counterpart of \code{\link[=brimmer_multiple]{brimmer_multiple()}} for the exact test.
}
\details{
Rows are \strong{grouped by design} and each group certified in a single
\code{\link[=brimmest]{brimmest()}} call, which is the whole point of the function: one attainable
lattice serves every report of a design and is built once, so certifying a
200-row table on one design costs barely more than certifying one row of it.
Repeated \verb{(mean, sd)} pairs within a design are computed once and reused.
Row order is preserved.

Recognised per row (column or constant): \code{l}, \code{u}, \code{n}, \code{mean}, \code{sd},
\code{digits}, \code{mean_digits}, \code{sd_digits}, \code{scoring}, \code{n_items}.

\code{rounding}, \code{max_cells} and \code{search_budget} may be given only as constants,
never as columns. \code{rounding} is a \emph{set} of admitted rules rather than one
value per row, and the other two are cost controls on the call; passing any
of them as a column is an error rather than a silent per-row reading.
}
\examples{
reports <- data.frame(mean = c(3.0, 1.3, 2.5),
                      sd   = c(1.0, 0.9, 1.2))

# one design, one lattice. Rows 2 and 3 are both impossible, for different
# reasons: row 3 already fails GRIM and GRIMMER, while row 2 clears every
# closed-form screen and is caught only here.
brimmest_multiple(reports, l = 1, u = 5, n = 9, digits = 1)

# designs may vary per row; rows sharing one are certified together
mixed <- data.frame(mean = c(3.0, 1.3, 4.0),
                    sd   = c(1.0, 0.9, 1.5),
                    n    = c(9, 9, 12))
brimmest_multiple(mixed, l = 1, u = 5, digits = 1,
                  include_inputs = FALSE)
}
\seealso{
\code{\link[=brimmest]{brimmest()}} for one design at a time, \code{\link[=brimmer_multiple]{brimmer_multiple()}} for the
closed-form screen to run first.
}
