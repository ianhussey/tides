% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brimmer.R
\name{brimmer_multiple}
\alias{brimmer_multiple}
\title{Check many reported (mean, SD, n) rows against the SD bounds}
\usage{
brimmer_multiple(data, ..., include_inputs = TRUE)
}
\arguments{
\item{data}{A data frame, one reported statistic set per row.}

\item{...}{Constant arguments applied to all rows.}

\item{include_inputs}{If TRUE (default), returns \code{data} column-bound to the
results; if FALSE, only the result columns (same row order), for drop-in
use inside a \code{dplyr::mutate()}/\code{purrr} pipeline.}
}
\value{
A data frame of the \code{\link[=brimmer]{brimmer()}} columns, one row per input
row (optionally with the inputs prepended).
}
\description{
Applies \code{\link[=brimmer]{brimmer()}} to each row of a data frame, de-duplicating
identical constraint tuples so the (possibly expensive) rounding-envelope is
computed once per distinct input and reused. Columns of \code{data} whose names
match \code{\link[=brimmer]{brimmer()}} arguments are taken per row; any argument given in
\code{...} is a constant broadcast to every row. Supplying one name both ways is
an error.
}
\details{
Recognised names (column or constant): \code{l}, \code{u}, \code{a}, \code{b}, \code{n}, \code{mean},
\code{mean_digits}, \code{sd}, \code{sd_digits}, \code{rounding}, \code{Z}, \code{scoring}, \code{n_items},
\code{alpha}.
}
\examples{
reports <- data.frame(mean = c(2.97, 3.51, 4.20),
                      sd   = c(2.83, 3.50, 0.90),
                      n    = c(30, 30, 30))

# the scale limits and reported precision are constant across rows;
# row 2 reports an SD above the ceiling for a 1-7 scale at n = 30
out <- brimmer_multiple(reports, l = 1, u = 7,
                                mean_digits = 2, sd_digits = 2)
out[, c("mean", "sd", "n", "consistent", "failed_tests")]
}
