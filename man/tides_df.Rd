% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tides_df.R
\name{tides_df}
\alias{tides_df}
\title{Apply the \code{tides()} function to each row of a data frame}
\usage{
tides_df(
  .data,
  mean = NULL,
  sd = NULL,
  n = NULL,
  min = NULL,
  max = NULL,
  n_items = NULL,
  digits = NULL,
  calculate_min_sd = NULL,
  method = NULL,
  approximate_bounds_range = 10
)
}
\arguments{
\item{.data}{A data frame or tibble.  Must contain columns named
“mean”, “sd”, “n”, “min”, “max”, “n_items”, “digits” and
“calculate_min_sd” if you do not override them below.}

\item{mean}{Optional.  A numeric vector or a column reference
in \code{.data} providing the group means.  Defaults to the
\code{.data$mean} column.}

\item{sd}{Optional.  A numeric vector or a column reference
in \code{.data} providing the standard deviations.  Defaults to
\code{.data$sd}.}

\item{n}{Optional.  A numeric vector or a column reference
in \code{.data} providing the sample sizes.  Defaults to
\code{.data$n}.}

\item{min}{Optional.  A numeric vector or a column reference
in \code{.data} providing the observed minima.  Defaults to
\code{.data$min}.}

\item{max}{Optional.  A numeric vector or a column reference
in \code{.data} providing the observed maxima.  Defaults to
\code{.data$max}.}

\item{n_items}{Optional.  A numeric vector or a column
reference for the number of items in the scale.  Defaults
to \code{.data$n_items}.}

\item{digits}{Optional.  An integer vector or a column
reference indicating how many decimal places to display.
Defaults to \code{.data$digits}.}

\item{calculate_min_sd}{Optional.  A logical vector or a
column reference indicating whether \code{tides()} should recalculate
a minimum standard deviation.  Defaults to
\code{.data$calculate_min_sd}.}

\item{method}{Optional.  A string vector or a
column reference indicating whether \code{tides()} should test the
strict SD bounds for only the supplied mean (i.e., implicitly
test for GRIM and GRIMMER consistency, when argument is set to
'exact'), or whether in the case of undefined SD bounds the
bounds of nearby means should be used (when argument is set to
'approximate').}
}
\value{
A tibble combining the original \code{.data} with the
output of \code{tides()} for each row.  The result will include
any columns returned by \code{tides()}, un-nested into regular
data-frame columns.
}
\description{
\code{tides_df()} takes a data frame (.data) containing columns for
mean, standard deviation, sample size, minimum, maximum,
number of items, display digits, and a flag for whether to
recalculate the minimum standard deviation.  It then applies
your existing \code{tides()} function to each row and binds the
results into the original data frame.
}
\details{
This function uses tidy-evaluation to allow unquoted column
names when piping.  If you omit any of the mapping arguments,
it will assume a column of the same name exists in \code{.data}.

Internally, \code{purrr::pmap_dfr()} applies \code{tides()} row by row,
then \code{tidyr::unnest()} expands the results into a standard
data-frame form.
}
\examples{
\dontrun{
dat <- data.frame(
  mean             = c(4.2, 4.2, 1.2, 1.4),
  sd               = c(0.5, 0.5, 0.5, 0.6),
  n                = c( 14,  14,  30,  35),
  min              = 1,
  max              = c(7, 7, 5, 7),
  n_items          = 1,
  digits           = 2,
  calculate_min_sd = TRUE,
  method           = c("exact", "approximate", "exact", "exact")
)

res <- dat |> tides_df()
print(res)
}

}
